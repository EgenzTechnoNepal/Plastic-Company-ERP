# Copy to `.env` next to docker-compose.yml if you want overrides.
# Defaults work for local Docker Desktop.

POSTGRES_DB=greenflow_erp
POSTGRES_USER=greenflow
POSTGRES_PASSWORD=greenflow

DJANGO_SECRET_KEY=docker-dev-insecure-change-me
SEED_DEMO=1
